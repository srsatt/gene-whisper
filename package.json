{
  "name": "gene-whisper",
  "private": true,
  "scripts": {
    "web:dev": "pnpm --filter \"apps/web\" dev",
    "web:build": "pnpm --filter \"apps/web\" build",
    "web:preview": "pnpm --filter \"apps/web\" preview",
    "devserver:start": "pnpm --filter devserver start",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
